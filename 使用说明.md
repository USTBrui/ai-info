# AI 日报 · 每日看板 —— 使用说明

> 一个完全免费的「AI 资讯每日看板」：每天自动抓取你关心的 AI 资讯、
> 论坛热帖、厂商更新和优惠活动，双击网页一眼看完。
> Author: wr

---

## 一、快速开始（3 步）

1. **双击 `一键刷新.bat`** —— 它会自动抓取一次资讯，然后自动打开看板网页。
2. 看板打开了，就完事了。
3. 以后每天想看，双击 `一键刷新.bat` 即可（或等云端自动更新，见第四节）。

> 第一次运行会自动安装两个小辅助库（约 10 秒）。装不上也没关系，程序照常能跑。

---

## 二、看板里都有什么

| 区域 | 内容 |
| --- | --- |
| 今日简报 | 一句话概括今天值得知道的事，右上角显示当前是「AI 智能摘要」还是「规则模式」 |
| 优惠活动 | 自动识别「免费、限免、折扣、赠送额度、降价」等内容，橙色卡片横排展示 |
| 今日要闻 | 全部资讯按重要度排序，同一件事被多个来源报道会自动合并并标注「N 个源报道」 |
| 论坛热议 | 大佬说、LinuxDo 的热帖，按回复数排序 |
| 厂商动态 | 点厂商标签（DeepSeek、智谱、Kimi……）看它今天有什么动静 |
| 更新日志 | 各厂商/工具的 changelog，按天排列，今天的会高亮 |
| 右侧栏 | 数据源健康状态、关注词命中统计、历史归档、导出 Markdown |

**常用操作**

- 点卡片标题 → 新窗口打开原文，自动标记已读
- 点 ☆ 收藏，点 ★ 取消；点右上角 ★ 图标只看收藏
- 按 `/` 快速搜索，按 `Esc` 清空，按 `S` 只看收藏
- 点右上角 ☾/☀ 切换深色/浅色
- 右侧「历史归档」可以回看之前任何一天
- 「导出 Markdown 日报」会把当前视图存成一个 `.md` 文件

> 收藏、已读、主题这些只存在你自己的浏览器里，清浏览器数据会丢失，请知悉。

---

## 三、开启 AI 智能摘要（推荐，每月 1~3 元）

没有 AI 的时候程序也能跑（自动变成「规则模式」），
但有了 AI 摘要后，重要度打分更准、每条都有一句话摘要、每日简报质量更高。

1. 打开 https://platform.deepseek.com/ 注册并充值（**充 5 块钱够用两三个月**）。
2. 在「API Keys」页面创建一个 Key（形如 `sk-xxxxxxxx`），复制下来。
3. 在本项目根目录（`一键刷新.bat` 所在的文件夹）新建一个文本文件，重命名为 **`.env`**
   （注意前面有个点，没有 .txt 后缀）。
4. 用记事本打开它，只写一行，保存：

   ```
   DEEPSEEK_API_KEY=sk-你的Key粘贴在这里
   ```

5. 重新双击 `一键刷新.bat`，简报右上角变成「AI 智能摘要」就成功了。

> `.env` 已被 `.gitignore` 排除，**永远不会被上传到网上**，放心。

---

## 四、上云端：让 GitHub 每天自动帮你更新（免费）

你本机网络连不上 LinuxDo、V2EX、GitHub（实测如此），
而 GitHub 的服务器在国外，这些站它全能抓。
所以**主用云端定时抓取，本地一键刷新作为补充**。

### 第 1 步：注册 GitHub

打开 https://github.com/signup 注册一个账号。

### 第 2 步：创建仓库

1. 登录后点右上角 **+** → **New repository**
2. 仓库名随便起（比如 `ai-info`）
3. 选 **Private（私有）** 或 **Public（公开）**：
   - **私有**：数据只有你能看。缺点是免费版不能开手机访问的网页。
   - **公开**：数据全网可见（都是公开资讯，没有隐私），可以免费开通手机访问。
4. 点 **Create repository**，创建完先停在那个页面。

### 第 3 步：把项目推上去

在项目文件夹里打开命令行（地址栏输入 `cmd` 回车），依次执行：

```bat
git init
git add -A
git commit -m "第一版：AI 资讯每日看板"
git branch -M main
git remote add origin https://github.com/你的用户名/ai-info.git
git push -u origin main
```

> 第一次 push 会弹窗让你登录 GitHub，跟着提示授权即可。
> 如果提示需要配置姓名邮箱，按提示执行那两条命令再重试。

### 第 4 步：填入 AI Key（可选）

仓库页面 → **Settings** → **Secrets and variables** → **Actions** →
**New repository secret**，Name 填 `DEEPSEEK_API_KEY`，Secret 填你的 Key，保存。

### 第 5 步：手动跑一次试试

仓库页面 → **Actions** 页签 → 左边选「**每日抓取**」→ 右边 **Run workflow** → **Run workflow**。

等两三分钟，变绿就是对勾成功。之后它会每天 **北京时间 8:00 / 14:00 / 20:00** 自动跑，
本地打开 `网页\index.html` 就是最新数据。

### 第 6 步（可选）：手机也能看

只有**公开仓库**能开。仓库页面 → **Settings** → **Pages** →
Source 选 **Deploy from a branch** → Branch 选 `main` / `/ (root)` → Save。

一两分钟后会得到一个网址 `https://你的用户名.github.io/ai-info/`，手机打开就是看板。

---

## 五、常见问题

**问：命令行输出中文乱码？**
程序内部已经强制 UTF-8。双击 `.bat` 文件时窗口会自动切编码，一般不会乱。
如果用别的方式运行报编码错误，先执行 `chcp 65001` 再运行。

**问：某几个源一直失败？**
双击 `探测数据源.bat` 看失败原因。本机连不上的（LinuxDo、V2EX、GitHub）是网络问题，
云端能抓到，不用管；确实想关掉某个源，在 `配置/数据源清单.json` 里把它的 `"启用"` 改成 `false`。

**问：怎么加新源？**
看《数据源配置说明.md》，三种类型的写法都有现成模板。

**问：怎么改抓取频率？**
编辑 `.github/workflows/每日抓取.yml` 里的 cron。
默认每天 3 次。改频繁了会多花 Actions 分钟数（免费额度每月 2000 分钟，目前用量约 450 分钟）。

**问：Actions 突然不跑了？**
GitHub 规定：仓库 60 天没有任何活动，定时任务会被自动暂停。
隔一两个月去 Actions 页面手动点一次 Run workflow 就能保活，或提交任何一次代码也会保活。

**问：DeepSeek 欠费了会怎样？**
自动降级成规则模式，看板照常工作，只是没有 AI 摘要。页面右上角会显示「规则模式」提醒你。

**问：想重置看板的所有本地记录？**
看板右侧栏有「恢复默认设置」按钮，一键清空收藏、已读、主题。

---

## 六、文件都是干嘛的

```
一键刷新.bat          ← 双击：抓取 + 打开看板
探测数据源.bat         ← 检查哪些源能抓、哪些失败
探测网页源.bat         ← 加新厂商源时帮你找选择器
网页\index.html       ← 看板本体（双击就能看）
配置\数据源清单.json   ← 所有信息源的总开关
配置\关注关键词.json   ← 你关心的词、优惠词、厂商别名
.env                 ← 你的 DeepSeek Key（不会上传）
日志\抓取日志.md      ← 每次运行的情况记录
快照\                ← 厂商页面的存档（别删，删了会导致重新建档）
src\                 ← Python 源代码
```
