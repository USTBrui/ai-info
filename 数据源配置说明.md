# 数据源配置说明

> 想让看板多抓点东西？照着这份说明在「配置/数据源清单.json」里加一段就行，不用改代码。
> Author: wr

---

## 一、三种源类型，怎么选

| 类型 | 什么时候用 | 例子 |
| --- | --- | --- |
| `rss` | 网站/博客/论坛提供订阅源（网址常以 `.rss`、`.xml`、`/feed` 结尾）| 大佬说、LinuxDo、机器之心 |
| `atom` | 专盯 GitHub 上的开源工具（版本发布、代码提交） | Claude Code、各种开源项目 |
| `html_diff` | 网站根本没有 RSS，就定期去看网页，把新出现的内容挑出来 | DeepSeek 更新日志、智谱新品页 |

**判断顺序**：先找 RSS（最省事）→ GitHub 项目用 atom → 都没有才用 html_diff。

---

## 二、三种类型的配置模板

### 1. rss（最常见）

```json
{
  "id": "qbitai",
  "名称": "量子位",
  "类型": "rss",
  "网址": "https://www.qbitai.com/feed",
  "分类": "行业新闻",
  "厂商": null,
  "启用": true,
  "选择器": [],
  "备注": "随便写点什么提醒自己"
}
```

### 2. atom（盯 GitHub 项目）

```json
{
  "id": "gh_claude_code",
  "名称": "Claude Code · 版本发布",
  "类型": "atom",
  "网址": "https://github.com/anthropics/claude-code/releases.atom",
  "分类": "开源项目",
  "厂商": "Claude Code",
  "启用": true,
  "选择器": [],
  "备注": "想盯代码提交就换成 commits/主分支名.atom"
}
```

> 把网址里的 `作者/仓库` 换成你想盯的任何 GitHub 项目即可，
> 比如 `https://github.com/ollama/ollama/releases.atom`。

### 3. html_diff（没有 RSS 的厂商页面）

```json
{
  "id": "deepseek_changelog",
  "名称": "DeepSeek · 更新日志",
  "类型": "html_diff",
  "网址": "https://api-docs.deepseek.com/zh-cn/updates",
  "分类": "更新日志",
  "厂商": "DeepSeek",
  "启用": true,
  "选择器": ["article h3", "main h3"],
  "备注": "选择器按顺序尝试，第一个抓不到自动试第二个"
}
```

**选择器怎么找？** 两步：

1. 用浏览器打开目标页面，按 `F12` → 点左上角箭头图标 → 点一下页面里某一条内容，
   看看它被什么标签包着（比如 `<h3>`、`<li class="item">`）。
2. 或者更省事：双击 `探测网页源.bat`，把网址给它，它会把每个候选选择器
   能抓到什么直接列出来给你挑。

**已实测可用的厂商页面**（直接抄就能用）：

| 厂商 | 网址 | 选择器 |
| --- | --- | --- |
| DeepSeek | `https://api-docs.deepseek.com/zh-cn/updates` | `["article h3"]` |
| 智谱 | `https://docs.bigmodel.cn/cn/update/new-releases` | `["[class*=update-]"]` |
| Kimi | `https://platform.kimi.ai/docs/platform-changelog` | `["main h2"]` |
| 阿里百炼 | `https://help.aliyun.com/zh/model-studio/models` | `["table tbody tr"]` |

> 注意：MiniMax、火山方舟、腾讯云文档这些页面靠 JavaScript 现渲染内容，
> 静态抓取只能拿到空壳，**做不了 html_diff 源**，别浪费时间。
> 这类厂商的动态可以靠「量子位」「Solidot」等新闻源间接覆盖。

---

## 三、所有字段说明

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | 唯一英文代号，不能和其他源重名 |
| `名称` | 是 | 显示在健康面板和时间线里，随便起 |
| `类型` | 是 | 只能填 `rss` / `atom` / `html_diff` 三种之一 |
| `网址` | 是 | 必须以 `http://` 或 `https://` 开头 |
| `分类` | 是 | 只能填：行业新闻 / 论坛热议 / 厂商动态 / 更新日志 / 优惠活动 / 开源项目 |
| `厂商` | 否 | 填了会出现在看板「厂商动态」区；没有填 `null` |
| `启用` | 是 | `true` 抓 / `false` 暂时不抓但保留配置 |
| `选择器` | — | 只有 `html_diff` 需要；是字符串数组，可以有多个备选 |
| `备注` | 否 | 写给自己看的话 |

---

## 四、容易踩的坑

1. **JSON 语法**：字符串必须双引号；最后一项后面**不能有逗号**；文件里不能写 `//` 注释。
   写错了程序会用中文告诉你错在第几行，跟着提示改就行。
2. **改完配置怎么生效**：不用重启什么，下次跑 `一键刷新.bat` 自动生效。
   改完可以先运行 `python src\config_loader.py` 检查一下写没写对。
3. **html_diff 第一天**：新加的网页源第一次运行时**不会产出任何内容**，
   它只是先把当前页面记下来当「底片」。从第二次运行起，新出现的内容才算数。
   这是故意的——否则第一天会把页面上所有历史内容都当成新闻。
4. **别当坏人**：程序已经做了礼貌抓取（浏览器标识、同一网站排队、每天只抓两三次）。
   不要把抓取频率改得很高，也不要去抓明显拒绝爬虫的网站。
